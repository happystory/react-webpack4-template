import React, { Component } from 'react';
import { hot } from 'react-hot-loader';
import './App.scss';

@hot(module)
class App extends Component {
  render() {
    return (
      <div>
        App
      </div>
    );
  }
}

export default App;
